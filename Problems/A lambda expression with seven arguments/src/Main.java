class Seven {

    public static MultipleArgumentsLambda.SeptenaryStringFunction fun = (a, b, c, d, e, f, g) -> {
        return a.toUpperCase() + b.toUpperCase() + c.toUpperCase() + d.toUpperCase() +
                e.toUpperCase() + f.toUpperCase() + g.toUpperCase();
    };
}